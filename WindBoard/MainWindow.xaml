<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="WindBoard.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WindBoard"
    xmlns:controls="using:WindBoard.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="WindBoard">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <CommandBar DefaultLabelPosition="Right">
            <AppBarButton x:Name="UndoButton" Icon="Undo" Label="撤销" />
            <AppBarButton x:Name="RedoButton" Icon="Redo" Label="重做" />
            <AppBarSeparator />
            <AppBarButton x:Name="ClearButton" Icon="Delete" Label="清空" />
            <AppBarToggleButton x:Name="EraserToggleButton" Icon="Edit" Label="擦除" />
            <AppBarSeparator />
            <AppBarButton x:Name="SaveButton" Icon="Save" Label="保存" IsEnabled="False" />
            <AppBarButton x:Name="ExportButton" Icon="OpenFile" Label="导出" IsEnabled="False" />
        </CommandBar>

        <controls:BoardCanvasControl x:Name="BoardCanvas" Grid.Row="1" />
    </Grid>
</Window>
