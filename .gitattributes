# 行尾符策略（仓库级规则，覆盖各贡献者本机的 core.autocrlf 设置）
#
# 背景：本仓库原先没有行尾规则，行为完全依赖各人本机的 core.autocrlf
# （当前工作副本为 local 设置 true），导致工作区行尾出现漂移：
# 643 个文件为 CRLF、185 个为 LF（多为较新创建、从未被重新检出的文件），
# 另有 3 个文件自身行尾混排。本文件把策略固化到仓库内，使行为不再取决于个人配置。
#
# 分工：
# - 本文件（git 层）：决定内容如何存入索引与如何检出到工作区。
# - 根目录 .editorconfig（编辑器层）：决定编辑器与工具写文件时使用的行尾。
#   两者在 Windows 上均落到 CRLF，保持一致。
#
# `* text=auto` 是 GitHub 官方推荐的默认写法：Git 自动判断文本/二进制，
# 并将文本内容在索引中统一规范化为 LF，检出时按平台决定。
# 未采用 `* text=auto eol=crlf`：官方明确不建议在非「所有平台都必须 CRLF」
# 的场景下使用该写法；此处只对 Windows 工具链要求 CRLF 的类型显式指定。
* text=auto

# Windows 工具链要求 CRLF 的文件类型（即使在其他平台检出也保持 CRLF）
*.sln     text eol=crlf
*.slnx    text eol=crlf
*.bat     text eol=crlf
*.cmd     text eol=crlf
*.props   text eol=crlf
*.targets text eol=crlf
*.vcxproj text eol=crlf
*.iss     text eol=crlf
*.isl     text eol=crlf

# 二进制文件不做行尾转换（`binary` 是 `-text -diff` 的别名）
*.png binary
*.ico binary
*.ttf binary

# Trellis: append-only developer journals should merge cleanly across
# parallel sessions/worktrees — each session only appends a new block, so
# there is nothing to actually conflict on.
#
# Do NOT add a rule for workspace/*/index.md here — it is fully regenerated
# every session, so a real conflict there is expected and safe to resolve by
# picking either side (task state lives in task.json, not index.md). See
# .trellis/spec/cli/backend/directory-structure.md for details.
.trellis/workspace/*/journal-*.md merge=union
